name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: Platane/snk@v3
        with:
          github_user_name: LizanLycan
          outputs: dist/github-contribution-grid-snake.svg
      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
# *(Once this runs, uncomment the "Snake Animation" section in the README code I provided).*

#### 2. **3D Contribution Graph**
# If you want something even flashier than the standard 2D graph.
# * **Tool:** [yoshi389105/github-profile-3d-contrib](https://github.com/yoshi389105/github-profile-3d-contrib)
# * **Why:** It turns your commit history into a 3D city block. It looks very "Metaverse/Web3".

#### 3. **Profile Trophy**
# Since you have a strong history, trophies add credibility.
# * **Tool:** [ryo-ma/github-profile-trophy](https://github.com/ryo-ma/github-profile-trophy)
# * **Code to add to README:**
#     ```html
#     <div align="center">
#       <img src="https://github-profile-trophy.vercel.app/?username=LizanLycan&theme=onestar&no-frame=true&margin-w=15" />
#     </div>

### **Final Touch: The Banner**
# In the README code I provided, there is a placeholder image link at the very top (`capsule-render`).
# * **Recommendation:** To be truly original, generate a custom banner using AI (Midjourney/DALL-E).
# * **Prompt Idea:** *"A cinematic wide shot of a futuristic cyber-wolf standing on a digital cliff overlooking a blockchain network city, neon blue and deep purple, high tech, 8k resolution."*
# * Upload that image to your repo and replace the `<img src="...">` link at the top.
